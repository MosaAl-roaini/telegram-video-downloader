# MosaVideoDownloaderBot

## التشغيل
1. افتح `.env` وضع التوكن الجديد من BotFather.
2. ثبّت المتطلبات: `python -m pip install -r requirements.txt`
3. شغّل: `python bot.py`

لا تشارك `.env` أو Bot Token. استخدم البوت فقط لتنزيل المحتوى الذي تملك حق تنزيله، ولا لتجاوز DRM أو الحماية.
